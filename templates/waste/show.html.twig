{% extends 'base.html.twig' %}

{% block title %}Waste{% endblock %}

{% block body %}
    <h1>Waste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ waste.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ waste.type }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ waste.location }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ waste.etat }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ waste.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_waste_index') }}">back to list</a>

    <a href="{{ path('app_waste_edit', {'id': waste.id}) }}">edit</a>

    {{ include('waste/_delete_form.html.twig') }}
{% endblock %}
