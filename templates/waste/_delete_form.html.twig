<form method="post" action="{{ path('app_waste_delete', {'id': waste.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ waste.id) }}">
    <button class="btn">Delete</button>
</form>
